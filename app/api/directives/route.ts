// Backward-compatible alias for the v0.4 web endpoint. Direct submissions now
// enter the scene ballot instead of bypassing arbitration into the generation FIFO.
export { POST } from "../proposals/route.ts";
